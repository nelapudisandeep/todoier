{
  "name": "target",
  "version": "1.0.0",
  "description": "this is a full stack todo app which will be made as a web app later.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nelapudi sandeep",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nedb": "^1.8.0"
  }
}
